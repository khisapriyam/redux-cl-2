import { BLUE, GRAY, GREEN, RED, YELLOW } from "./actionType"

//make red
export const makeRed = (payload) => { //payload use korbo jodi baite theke data ase
    return {
        type : RED
    }
}
//make red
export const makeGreen = (payload) => { //payload use korbo jodi baite theke data ase
    return {
        type : GREEN
    }
}
//make red
export const makeYellow = (payload) => { //payload use korbo jodi baite theke data ase
    return {
        type : YELLOW
    }
}
//make red
export const makeBlue = (payload) => { //payload use korbo jodi baite theke data ase
    return {
        type : BLUE
    }
}
//make red
export const makeGray = (payload) => { //payload use korbo jodi baite theke data ase
    return {
        type : GRAY
    }
}